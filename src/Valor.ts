export class Valor {
    nombre: string;
    unidad_medida: string;

    constructor(nombre: string, unidad_medida: string) {
        this.nombre = nombre;
        this.unidad_medida = unidad_medida;
    }
}